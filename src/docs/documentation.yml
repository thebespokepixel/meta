toc:
  - name: "Usage"
    example: src/docs/example.md
  - name: "API"
    description: |
      Look up the file system hierarchy, starting from the current working
      directory or provided path for a package file and return specific package
      metadata as a sanitised map.
